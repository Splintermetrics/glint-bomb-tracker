# Glint Bomb Tracker

A lightweight dashboard for the Splinterlands `players/glint_bomb_prizes` API endpoint.

## Run locally

Serve this folder with any static server, then open it in a browser. For example:

```powershell
npx serve .
```

The dashboard defaults to `doombot75`. Enter another Splinterlands username to fetch that player's live prize history.
